import 'bootstrap/dist/css/bootstrap.min.css';

export const metadata = {
  title: 'Impression Bordereau',
};

export default function ImpressionBordereauLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
